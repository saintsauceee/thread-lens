apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: thread-lens
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - name: worker
          image: thread-lens-worker:dev
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: thread-lens-config
            - secretRef:
                name: thread-lens-secrets
